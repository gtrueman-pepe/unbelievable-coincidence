import Link from 'next/link';import type { Story } from '@/lib/supabase';
export function StoryShell({stories,featured}:{stories:Story[],featured?:Story}){return <main className="story-grid"><aside className="index"><div className="eyebrow">The stories</div><nav>{stories.map((s,i)=><Link key={s.id} href={`/stories/${s.slug}`}><span>{String(i+1).padStart(2,'0')}</span>{s.title}</Link>)}</nav><Link href="/submit" className="submit-link">Tell us your story <b>→</b></Link></aside><article className="feature">{featured?<><div className="eyebrow">A story selected by chance</div><h1>{featured.title}</h1><p className="byline">Submitted by {featured.byline}</p><div className="story-body">{featured.body.split('\n\n').map((p,i)=><p key={i}>{p}</p>)}</div></>:<div className="empty"><div className="eyebrow">The first coincidence awaits</div><h1>A curious beginning</h1><p>True stories will appear here after they have been submitted, verified, and approved.</p></div>}</article></main>}
