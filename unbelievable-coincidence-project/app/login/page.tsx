'use client';import {useState} from 'react';import {publicDb} from '@/lib/supabase';
export default function Login(){const [sent,setSent]=useState(false);async function login(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const email=new FormData(e.currentTarget).get('email') as string;await publicDb().auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/auth/callback`}});setSent(true)}return <main className="login"><div className="eyebrow">Owner access</div><h1>Editorial desk</h1>{sent?<p>Check your email for the secure sign-in link.</p>:<form onSubmit={login}><label>Email<input type="email" name="email" required/></label><button>Send sign-in link</button></form>}</main>}
