'use client';import {useState} from 'react';import Script from 'next/script';
export function SubmitForm(){const [state,setState]=useState<'idle'|'sending'|'sent'|'error'>('idle');const [count,setCount]=useState(0);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('sending');const form=new FormData(e.currentTarget);const r=await fetch('/api/submit',{method:'POST',body:form});setState(r.ok?'sent':'error');if(r.ok)e.currentTarget.reset();}if(state==='sent')return <div className="notice"><div className="eyebrow">Check your inbox</div><h2>One small step remains.</h2><p>We sent a verification link to your email. Your story will enter review after you confirm it.</p></div>;return <form onSubmit={submit} className="submission-form"><label>Story title <span>80 characters</span><input name="title" required maxLength={80}/></label><fieldset><legend>How should we credit you?</legend><div className="radio-row"><label><input type="radio" name="creditType" value="name" defaultChecked/> Name</label><label><input type="radio" name="creditType" value="initials"/> Initials</label><label><input type="radio" name="creditType" value="anonymous"/> Anonymous</label></div></fieldset><label>Name or initials <span>Not shown if anonymous</span><input name="byline" maxLength={60}/></label><label>Email address <span>Private; verification required</span><input name="email" type="email" required autoComplete="email" maxLength={254}/></label><label>Your true story <span>{count.toLocaleString()} / 8,000</span><textarea name="body" required maxLength={8000} rows={14} onChange={e=>setCount(e.target.value.length)}/></label><label className="consent"><input type="checkbox" name="consent" value="yes" required/><span>I affirm that this story is true to the best of my knowledge and grant permission to publish and lightly edit it.</span></label><div className="cf-turnstile" data-sitekey={process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY}/><button disabled={state==='sending'}>{state==='sending'?'Submitting…':'Submit for consideration'}</button>{state==='error'&&<p className="error" role="alert">Something went wrong. Please check the form and try again.</p>}<Script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer/></form>}
